{
  "name": "sda-cms-certification-service",
  "private": true,
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "postbuild": "rm -rf dist/assets && cp -r assets dist/ && cp package.json dist/",
    "dev": "tsc --watch & NODE_ENV=dev nodemon dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint -c ./tslint.json './src/**/*.ts*'"
  },
  "dependencies": {
    "@types/koa": "^2.0.39",
    "@types/winston": "^2.3.5",
    "canvas": "~2.6.1",
    "koa": "^2.8.1",
    "koa-body": "^4.1.1",
    "moment": "^2.18.1",
    "winston": "^2.3.1"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "nodemon": "^2.0.6"
  }
}
